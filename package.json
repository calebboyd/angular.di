{
  "name": "angular.di",
  "version": "4.1.1",
  "description": "angular di as a standalone package",
  "scripts": {
    "bump": "node bump",
    "bundle": "rm -rf dist && node copy-types && webpack",
    "test": "npm run bundle && ts-node test",
    "prepublish": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/calebboyd/angular.di"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "main": "dist/bundle.js",
  "typings": "dist/bundle.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@angular/core": "4.1.1",
    "bluebird": "^3.5.0",
    "reflect-metadata": "^0.1.10",
    "rxjs": "^5.3.1",
    "ts-loader": "^2.0.3",
    "ts-node": "^3.0.4",
    "typescript": "^2.3.2",
    "webpack": "^2.5.1"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.1.10"
  }
}
