{
  "name": "angular.di",
  "version": "2.4.1",
  "description": "angular di packaged apart from @angular/core",
  "scripts": {
    "bump": "node bump",
    "bundle": "rm -rf dist && node copy-types && webpack",
    "test": "npm run bundle && ts-node test",
    "prepublish": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/calebboyd/angular.di"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "main": "dist/bundle.js",
  "typings": "dist/bundle.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@angular/core": "2.4.1",
    "bluebird": "^3.4.6",
    "rxjs": "^5.0.1",
    "ts-loader": "^1.3.3",
    "typescript": "^2.1.4",
    "webpack": "^2.2.0-rc.1"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.1.9"
  }
}
